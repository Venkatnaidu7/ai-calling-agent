# AI runtime

The OpenAI Realtime session is created server-side. The model receives trusted system instructions and backend-approved tools; customer speech and retrieved knowledge are treated as untrusted input.

The voice bridge supports server-side VAD and interruption cancellation. Tool execution is performed by the backend after tenant and agent-version authorization.
